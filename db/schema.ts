import {sqliteTable,text,integer} from 'drizzle-orm/sqlite-core';
export const products=sqliteTable('products',{id:text('id').primaryKey(),name:text('name').notNull(),category:text('category').notNull(),description:text('description').notNull(),image:text('image').notNull().default(''),variants:text('variants').notNull().default(''),unit:text('unit').notNull().default('item'),price:text('price').notNull().default(''),availability:text('availability').notNull().default('Confirm on quotation'),featured:integer('featured').notNull().default(0)});
export const categories=sqliteTable('categories',{name:text('name').primaryKey(),items:text('items').notNull(),icon:text('icon').notNull()});
export const settings=sqliteTable('settings',{id:text('id').primaryKey(),value:text('value').notNull()});
export const quotations=sqliteTable('quotations',{id:text('id').primaryKey(),customerName:text('customer_name').notNull(),company:text('company'),phone:text('phone').notNull(),whatsapp:text('whatsapp').notNull(),email:text('email'),location:text('location').notNull(),products:text('products').notNull(),notes:text('notes'),requirements:text('requirements'),preferredDate:text('preferred_date'),createdAt:text('created_at').notNull(),status:text('status',{enum:['New','Reviewing','Quoted','Accepted','Completed','Cancelled']}).notNull().default('New')});
